"""labkit — programmatise the experimental workflow.

`labkit new` scaffolds a fully cross-linked experiment object (data/analysis folder + Obsidian
lab note + analysis stub) from a template seeded with the project's standing context — so you
never hand-make a Finder directory. `labkit index` regenerates a Mission Control dashboard note
from every experiment's frontmatter (the visible glue; no Obsidian plugin required).
"""
__version__ = "0.1.0"
